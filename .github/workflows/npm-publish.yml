name: Release - NPM Package

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (e.g. 1.2.3, 1.2.3-beta.1)"
        required: true
        type: string
      commit:
        description: "Commit hash to stamp (defaults to HEAD)"
        required: false
        type: string
        default: ""
      dry-run:
        description: "Dry run (pack only, do not publish)"
        required: false
        type: boolean
        default: false
  workflow_call:
    inputs:
      version:
        description: "Version to publish"
        required: true
        type: string
      commit:
        description: "Commit hash to stamp"
        required: false
        type: string
        default: ""
      dry-run:
        description: "Dry run (pack only, do not publish)"
        required: false
        type: boolean
        default: false
    secrets:
      NPM_TOKEN:
        description: "npm authentication token"
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - uses: oven-sh/setup-bun@v2

      - uses: actions/cache@v5
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - run: bun install --frozen-lockfile

      # Verification gates
      - run: bun run check
      - run: bun run typecheck
      - run: bun test

      # Build web assets and stage the npm artifact
      - name: Stage npm artifact
        env:
          VERSION: ${{ inputs.version }}
          COMMIT: ${{ inputs.commit }}
        run: |
          bun run build:web
          bun run --filter @cookielab.io/klovi build
          bun apps/package/scripts/stage-npm.ts --version "$VERSION" --commit "${COMMIT:-$(git rev-parse --short HEAD)}"

      # Verify the staged artifact
      - name: Verify staged artifact manifest
        run: |
          echo "=== Staged package.json ==="
          cat apps/package/.stage/npm/package.json
          echo ""
          echo "=== Checking for workspace deps ==="
          if grep -q 'workspace:' apps/package/.stage/npm/package.json; then
            echo "ERROR: workspace:* dependencies found in staged manifest"
            exit 1
          fi
          echo "No workspace dependencies found - OK"

      # Run packed-artifact verification
      - name: Verify packed artifact (Node + Bun)
        run: bun run verify:packed-artifact

      # Publish from staged directory only.
      # Authentication: uses secret-based NPM_TOKEN. To adopt npm trusted publishing
      # (provenance-based OIDC), replace NODE_AUTH_TOKEN with the npm provenance flag
      # and configure the npm package for trusted publishing in the npm registry.
      - name: Publish to npm
        if: inputs.dry-run == false
        working-directory: apps/package/.stage/npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public

      - name: Dry-run pack
        if: inputs.dry-run == true
        working-directory: apps/package/.stage/npm
        run: npm pack --dry-run
