{
  "name": "@cookielab.io/klovi-web",
  "version": "0.0.0",
  "private": true,
  "description": "Shared application UI for Klovi",
  "type": "module",
  "scripts": {
    "dev": "bun build src/main.tsx --outdir dist --watch",
    "build": "bun build src/main.tsx --outdir dist --minify",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@cookielab.io/klovi-design-system": "workspace:*",
    "@cookielab.io/klovi-ui": "workspace:*",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
